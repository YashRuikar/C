// write a progarm to determine wheter the number is divisible by 97 or not

#include <stdio.h>

int main()
{
    int number;
    printf("Enter the value of number\n");
    scanf("%d", &number);
    printf("Divisibility test returns: %d\n", number % 97);
    

// Q4 step for evalution of 3*x/y-z+k

    int x = 2, y = 3, z = 3, k = 1;
    int result = 3 * x / y - z + k;
    printf("The value of result is %d", result);

    return 0;
}

